{% extends 'FlexyDatabaseConfigBundle::layout.html.twig' %}

{% trans_default_domain 'FlexyDatabaseConfigBundle' %}

{% block body -%}
    <h1>{{ 'role.new.title'|trans }}</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('flexy_role') }}">
            {{ 'role.list.back'|trans }}
        </a>
    </li>
</ul>
{% endblock %}
